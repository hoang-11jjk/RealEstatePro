{
  "name": "realestatepro-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "json-server --watch db.json --port 3000",
    "start": "node ./node_modules/json-server/lib/cli/bin.js --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": { "node": ">=18" }
}
